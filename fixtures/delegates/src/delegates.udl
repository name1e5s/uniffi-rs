namespace delegates {};

[Delegate]
interface MyDelegate {
  string with_return();
  void   without_return();
};

[Delegate=MyDelegate]
interface RustObject {
  constructor();

  [Name=from_string]
  constructor(string s);

  [CallWith=without_return]
  string identity_string(string s);
};